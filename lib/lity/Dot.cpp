#include "Dot.h"

bool Dot::find(const Fields& fields, const FieldPoint& fieldPoint) const {
    return false;
}

auto Dot::apply(Fields& fields, const FieldPoint& fieldPoint) const -> AnimationList {
    return AnimationList();
}

#include "Arrowhead.h"

bool Arrowhead::find(const Fields& fields, const FieldPoint& fieldPoint) const {
    return false;
}

auto Arrowhead::apply(Fields& fields, const FieldPoint& fieldPoint) const -> AnimationList {
    return AnimationList();
}
